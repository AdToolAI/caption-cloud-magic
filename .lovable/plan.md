

## Plan: Videoübersetzer von "Erstellen" nach "Optimieren" verschieben

### Änderung

In `src/config/hubConfig.ts` wird der Eintrag `{ route: "/video-translator", ... }` aus dem `erstellen`-Hub entfernt und in den `optimieren`-Hub verschoben.

### Betroffene Datei

`src/config/hubConfig.ts` — eine Zeile verschieben

