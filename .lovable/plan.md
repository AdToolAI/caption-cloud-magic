
Problem
- Es sind sehr wahrscheinlich 2 getrennte Ursachen gleichzeitig:
  1. Dein Browser lädt noch einen alten Frontend-Stand. Im aktuellen Code ist der kompakte Phasen-Stepper bereits vorhanden und die fehlenden `stepProductImages`-Übersetzungen sind ebenfalls schon im Repository. Wenn du trotzdem noch die alte lange Leiste siehst, kommt das nicht mehr aus dem aktuellen Source-Code.
  2. Zusätzlich hat der Wizard selbst einen echten Zustandsfehler: Er stellt alte Drafts über einen rohen numerischen `step` wieder her. Wenn dieser Index nach den Step-Änderungen nicht mehr passt, landet der Wizard auf einem ungültigen oder leeren Zustand. Genau dann entsteht die große schwarze Fläche ohne Play-Button.

Do I know what the issue is?
- Ja: veralteter ausgelieferter Frontend-Build plus fehlende State-Normalisierung/Fallbacks im Wizard.

Plan
1. Service Worker als Ursache für alte UI ausschalten
- Die globale Registrierung in `src/main.tsx` entfernen.
- `sw.js` nur noch dann registrieren, wenn ein Nutzer Push-Benachrichtigungen wirklich aktiviert (`src/hooks/usePushNotifications.ts`).
- `public/sw.js` push-only lassen, ohne Request-Caching.
- So verhindern wir, dass der normale App-Flow weiter alte Wizard-Bundles ausliefert.

2. Draft-Wiederherstellung robust machen
- In `src/lib/universal-video-draft.ts` die Draft-Version erhöhen.
- Nicht nur `step`, sondern auch `stepId` speichern.
- Alte Drafts beim Wiederherstellen nicht blind übernehmen, sondern normalisieren oder verwerfen.
- Dadurch können alte Wizard-Stände nach Step-Änderungen nicht mehr ins Leere zeigen.

3. Safe-Step-Resolver in den Wizard einbauen
- In `src/components/universal-video-creator/UniversalVideoWizard.tsx` eine zentrale Logik ergänzen, die den gültigen Schritt aus dem vorhandenen Zustand ableitet:
  - keine Kategorie -> `category`
  - Produkt-Ad ohne Bilder -> `product-images`
  - Modus gewählt, aber noch kein Konzept -> `consultation`
  - Preview/Export ohne Projekt -> Recovery oder zurück
  - ungültiger/out-of-range Step -> auf gültigen Step zurücksetzen
- Diese Prüfung beim Laden, nach Draft-Resume und nach Recovery laufen lassen.

4. Schwarze Preview-Fläche direkt absichern
- Für `preview` ohne `autoGeneratedProject` eine echte Fallback-Ansicht anzeigen statt gar nichts.
- Zusätzlich einen generischen Fallback für unbekannte `currentStepId`-Zustände einbauen.
- Projektobjekte vereinheitlichen, damit `outputUrl`, `output_url` und `video_url` alle akzeptiert werden.
- Wenn ein fertiger Render im Backend existiert, wird er sauber geladen; wenn nicht, sieht der Nutzer klare Recovery-Aktionen statt einer leeren schwarzen Fläche.

5. Sichtbare Leiste final stabilisieren
- Sobald der alte Bundle-Pfad weg ist, wird der bereits vorhandene kompakte Phasen-Stepper aus `UniversalVideoWizard.tsx` auch wirklich live erscheinen.
- Falls nötig, ergänze ich kurzzeitig einen kleinen Build-Marker zur Kontrolle, damit sofort sichtbar ist, dass der frische Frontend-Stand geladen wurde.

Betroffene Dateien
- `src/main.tsx`
- `src/hooks/usePushNotifications.ts`
- `public/sw.js`
- `src/lib/universal-video-draft.ts`
- `src/components/universal-video-creator/UniversalVideoWizard.tsx`
- `src/lib/translations.ts` nur falls neue Recovery-Texte gebraucht werden

Erwartetes Ergebnis
- Kein leerer schwarzer Bereich mehr
- Entweder echter Preview-Player mit Play-Möglichkeit oder eine klare Recovery-Ansicht
- Alte Drafts können den Wizard nicht mehr in einen kaputten Zustand bringen
- Die neue kompakte Phasenleiste wird tatsächlich ausgeliefert statt der alten langen Step-Leiste
