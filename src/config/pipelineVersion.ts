/**
 * Canonical version number of the Briefing → Storyboard pipeline
 * running on the client. Bump on every meaningful pipeline change
 * so bug reports can be attributed unambiguously.
 *
 * The server also reports its own version in `_meta.debug.version`;
 * the debug chip renders both side-by-side.
 */
export const CLIENT_PIPELINE_VERSION = 220;
