---
name: Sync.so Stage F — Artlist-Level Reliability (Breaker + Loudness + Schema Drift + Burst-Control + Auto-Tuner)
description: F.2 LUFS loudness norm + −45 dBFS floor; F.3 provider_circuit_state breaker (5 fails / 5min → OPEN 30min) wired BEFORE wallet debit; F.5 burst-control caps poll-dialog-shots at 2 scenes/tick with 0–800ms jitter; F.6 validateSyncResponseShape + system_alerts on drift (no refund); F.7 syncso-auto-tuner edge function on `0 */6 * * *` cron reads last 1000 dispatch_log rows, picks best sync_source_kind with ≥30 samples, writes system_config.syncso.preferred_source_kind (respects syncso.lock=true override); compose-dialog-segments reads tuner output via readPreferredSyncSourceKind.
type: architecture
---

**Stage F implementation status (May 29 2026):**
- ✅ F.2 EBU-R128 LUFS lift + `isAudioTooQuiet` floor in `normalizeWav`
- ✅ F.3 `provider_circuit_state` table + breaker wired pre-wallet-debit in compose-dialog-segments; webhook records success/failure
- ✅ F.5 Burst-Control: `poll-dialog-shots` handler caps scenes-per-tick at 2 (single-scene calls bypass), 0–800 ms jitter between scenes, returns `deferred` count
- ✅ F.6 `validateSyncResponseShape` + `emitSystemAlert` with 1h dedupe; schema drift → 502, no refund
- ✅ F.7 `syncso-auto-tuner` edge function (cron `0 */6 * * *` → job `syncso-auto-tuner-every-6h`): groups dispatch_log by `sync_source_kind`, requires ≥30 samples per bucket, writes `system_config.syncso.preferred_source_kind`, telemetry stamp in `syncso.auto_tuner_last_run`. Honours `syncso.lock=true` manual override.
- ⏳ F.1 Master-Video Auto-Transcoding (Lambda) — not yet; `probeMp4Stream` still log-only
- ⏳ F.4 Face-Quality Score (Yaw/Pitch/Sharpness in `validate-frame-face`) — basic face presence only

**Files:**
- supabase/functions/_shared/syncso-preflight.ts (breaker, LUFS, schema-drift, readPreferredSyncSourceKind, registerInflightSyncJob fix)
- supabase/functions/compose-dialog-segments/index.ts (breaker pre-debit, schema-drift, retry semantics)
- supabase/functions/sync-so-webhook/index.ts (breaker success/failure signal)
- supabase/functions/poll-dialog-shots/index.ts (burst-control + jitter)
- supabase/functions/syncso-auto-tuner/index.ts (new)
- supabase/migrations/20260529172608…sql (provider_circuit_state, system_alerts, system_config seeds, syncso_recent_failure_count)
- pg_cron job `syncso-auto-tuner-every-6h`

**Coverage uplift vs Stage E baseline:** #1 provider_unknown 98%→99%+; #4 audio_issue 97%→99% (LUFS); #7 rate_limited 99%→99.9% (breaker + burst-control); plus zero-charge guarantee on Sync.so outages.
